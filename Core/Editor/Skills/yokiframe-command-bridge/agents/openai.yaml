interface:
  display_name: "YokiFrame Command Bridge"
  short_description: "YokiFrame 文件命令桥调试手册"
  default_prompt: "Use $yokiframe-command-bridge to inspect YokiFrame runtime Kit state through .yokiframe command files, while treating TableKit and GraphKit as Tauri editor tool flows rather than runtime command handlers."
